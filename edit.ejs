<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Chat - TalkBox</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="form-container">
    <h1>✏️ Edit Chat</h1>
    
    <div class="chat-info">
      <p><strong>From:</strong> <%= chat.from %></p>
      <p><strong>To:</strong> <%= chat.to %></p>
      <p><strong>Date:</strong> <%= chat.created_at.toLocaleString() %></p>
    </div>

    <form action="/chats/<%= chat._id %>?_method=PUT" method="POST">
      <div class="form-group">
        <label for="message">Message:</label>
        <textarea id="message" name="message" required><%= chat.message %></textarea>
      </div>

      <div class="btn-group">
        <a href="/chats" class="btn-back">← Cancel</a>
        <button type="submit" class="btn-submit btn-update">Update ✓</button>
      </div>
    </form>
  </div>
</body>
</html>